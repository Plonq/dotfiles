- clean: ['~']

- link:
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.config/qutebrowser/autoconfig.yml:
        path: config/qutebrowser/autoconfig.yml
        create: true

